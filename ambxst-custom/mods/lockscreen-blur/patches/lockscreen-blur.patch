diff --git a/modules/lockscreen/LockScreen.qml b/modules/lockscreen/LockScreen.qml
index e3f48446..76912e71 100644
--- a/modules/lockscreen/LockScreen.qml
+++ b/modules/lockscreen/LockScreen.qml
@@ -59,7 +59,7 @@ WlSessionLockSurface {
         layer.effect: MultiEffect {
             blurEnabled: true
             blur: startAnim ? 1 : 0
-            blurMax: 64
+            blurMax: 128
         }
 
         // Zoom animation
@@ -80,15 +80,23 @@ WlSessionLockSurface {
         }
     }
 
-    // Screen capture background (fondo absoluto con zoom sincronizado)
+    // Screen capture background
     ScreencopyView {
         id: screencopyBackground
         anchors.fill: parent
         captureSource: root.screen
         live: false
         paintCursor: false
-        visible: startAnim  // Visible solo cuando startAnim es true
-        z: 0  // Capa más baja - fondo absoluto
+        visible: startAnim
+        z: 0
+
+        // Blur real sobre la captura del escritorio
+        layer.enabled: true
+        layer.effect: MultiEffect {
+            blurEnabled: true
+            blur: startAnim ? 1.0 : 0.0
+            blurMax: 64
+        }
 
         property real zoomScale: startAnim ? 1.25 : 1.0
 
@@ -101,6 +109,7 @@ WlSessionLockSurface {
 
         Behavior on zoomScale {
             enabled: Config.animDuration > 0
+
             NumberAnimation {
                 duration: Config.animDuration * 2
                 easing.type: Easing.OutExpo
